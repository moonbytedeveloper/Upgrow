@model VerifyIndiaAdminPanel.Models.MasterEmailTemplateVM
@{
    var isEdit = Model?.EmailTemplate != null && !string.IsNullOrEmpty(Model.EmailTemplate.UUID);
    ViewData["Title"] = isEdit ? "Edit Email Template" : "Add Email Template";
}
@section HighPriority {
    <link href="~/assets/css/select2.min.css" rel="stylesheet" type="text/css" />
}



        <div class="container-fluid">
            <!-- Page-Title -->
            <div class="row mt-2">

                <div class="card">
                    <div class="card-header">
                        <div class="row align-items-center">
                            <div class="col">
                                <h3 class="card-title fs-20"> @ViewData["Title"]</h3>
                            </div><!--end col-->

                        </div> <!--end row-->
                    </div><!--end card-header-->
                    <div class="card-body pt-0">
                        <form id="bannerForm" asp-action="MasterAddEmailTemplate" method="post">
                            <input type="hidden" asp-for="EmailTemplate.UUID" />
                            <input type="hidden" asp-for="EmailTemplate.IsActive" />

                            <div class="row align-items-center">
                                <div class="col-lg-3 col-md-4 col-sm-6 col-12">
                                    <form-field asp-for="EmailTemplate.EmailCredentialUUID"
                                                label="Email Credential"
                                                type="Select"
                                                asp-items="@Model.EmailCredentialList"
                                                required="true"
                                                placeholder="Select Email Credential"  />
                                </div>
                                <div class="col-lg-3 col-md-4 col-sm-6 col-12">
                            <form-field asp-for="EmailTemplate.EmailTemplateName"
                                        label="Email Template Name"
                                        type="Text"
                                        required="true" />
                                </div>

                                <div class="col-lg-3 col-md-4 col-sm-6 col-12">
                                    <form-field asp-for="EmailTemplate.EmailSubject"
                                                label="Email Subject"
                                                type="Text"
                                                required="true" />
                                </div>
                                <div class="col-lg-12 col-sm-6 col-xs-12 pt-3">
                                    <form-field asp-for="EmailTemplate.Description"
                                                label="Description"
                                                type="CKEditor"
                                                required="true" />
                                </div>

                                <div class="col-lg-12 col-md-4 col-sm-6 col-12 text-align-end pt-4">
                                    <form-button label="Cancel"
                                                 type="Cancel"
                                                 url="@Url.Action("MasterViewEmailTemplate", "Master")" />
                                    <form-button label="Submit"
                                                 type="Submit" />
                                </div>

                            </div>
                        </form>
                    </div><!--end card-body-->
                </div><!--end card-->
            </div><!--end row-->
        </div><!-- container -->
        <!-- Timer -->
        <partial name="_Timer" />

<!-- end page-wrapper -->
@section Scripts
{
    <script src="~/ckeditor/ckeditor/ckeditor.js"></script>
    <script>
        // Keep your existing initialization script (unchanged)
        function initializeForm() {
    if (typeof jQuery === 'undefined') {
        setTimeout(initializeForm, 100);
        return;
    }
    if (typeof CKEDITOR !== 'undefined') {
        document.querySelectorAll('.ckeditor-textarea').forEach(function (textarea) {
            if (!CKEDITOR.instances[textarea.id]) {
                var editor = CKEDITOR.replace(textarea.id, {
                    extraAllowedContent: 'div(*)[*]{*}; span(*)[*]{*}; p(*)[*]{*};',
                    allowedContent: true,
                    height: 300,
                    toolbar: [
                        { name: 'document', items: ['Source', '-', 'Save', 'NewPage', 'Preview'] },
                        { name: 'clipboard', items: ['Cut', 'Copy', 'Paste', 'PasteText', 'PasteFromWord', '-', 'Undo', 'Redo'] },
                        { name: 'editing', items: ['Find', 'Replace', '-', 'SelectAll'] },
                        '/',
                        { name: 'basicstyles', items: ['Bold', 'Italic', 'Underline', 'Strike', 'Subscript', 'Superscript', '-', 'RemoveFormat'] },
                        { name: 'paragraph', items: ['NumberedList', 'BulletedList', '-', 'Outdent', 'Indent', '-', 'Blockquote'] },
                        { name: 'links', items: ['Link', 'Unlink'] },
                        { name: 'insert', items: ['Image', 'Table', 'HorizontalRule'] },
                        '/',
                        { name: 'styles', items: ['Styles', 'Format', 'Font', 'FontSize'] },
                        { name: 'colors', items: ['TextColor', 'BGColor'] }
                    ]
                });

                editor.on('change', function () {
                    editor.updateElement();
                });
            }
        });
    }
}

        document.addEventListener('DOMContentLoaded', initializeForm);
        if (typeof jQuery !== 'undefined') {
            jQuery(document).ready(function () {
                initializeForm();

                // Tell jQuery Validate to ignore nothing, so it can see the hidden textarea
                if ($.validator) {
                    $.validator.setDefaults({
                        ignore: []
                    });
                }
            });
        }
    </script>

    <partial name="_ValidationScriptsPartial" />
    <script src="~/assets/js/select2.min.js"></script>
    <script src="~/assets/js/validationanimation.js"></script>
}